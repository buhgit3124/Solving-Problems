#include <bits/stdc++.h>

using namespace std;

int main()
{
    int size;
    cin >> size;
    int arr[size];
    int *newArray = new int[size];

    delete[]newArray;
    newArray=NULL;

    return 0;
}
